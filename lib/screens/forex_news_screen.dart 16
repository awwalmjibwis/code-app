import 'package:flutter/material.dart';

class ForexNewsScreen extends StatelessWidget {
  const ForexNewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "Forex News",
          style: TextStyle(
            fontWeight: FontWeight.bold,
          ),
        ),
        centerTitle: true,
      ),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: [
          const Text(
            "Latest Market News",
            style: TextStyle(
              fontSize: 22,
              fontWeight: FontWeight.bold,
            ),
          ),

          const SizedBox(height: 8),

          const Text(
            "Stay updated with important Forex, Gold and Crypto news.",
            style: TextStyle(
              color: Colors.white70,
            ),
          ),

          const SizedBox(height: 20),

          _newsCard(
            icon: Icons.monetization_on,
            iconColor: Colors.amber,
            category: "GOLD",
            title: "Gold market remains in focus",
            description:
                "Traders continue to monitor economic data and central bank decisions that may influence gold prices.",
            time: "Today",
          ),

          _newsCard(
            icon: Icons.attach_money,
            iconColor: Colors.green,
            category: "FOREX",
            title: "USD market outlook",
            description:
                "The US Dollar remains sensitive to inflation data, employment reports and interest rate expectations.",
            time: "Today",
          ),

          _newsCard(
            icon: Icons.public,
            iconColor: Colors.blue,
            category: "EUR/USD",
            title: "Euro and Dollar under pressure",
            description:
                "EUR/USD traders are watching upcoming economic releases for possible changes in market direction.",
            time: "Today",
          ),

          _newsCard(
            icon: Icons.currency_pound,
            iconColor: Colors.purple,
            category: "GBP",
            title: "GBP traders watch economic data",
            description:
                "Important UK economic reports may create additional volatility in GBP pairs.",
            time: "Yesterday",
          ),

          _newsCard(
            icon: Icons.currency_bitcoin,
            iconColor: Colors.orange,
            category: "CRYPTO",
            title: "Bitcoin remains highly volatile",
            description:
                "Crypto traders continue to monitor Bitcoin price action and major market levels.",
            time: "Yesterday",
          ),

          const SizedBox(height: 20),

          const Card(
            child: Padding(
              padding: EdgeInsets.all(16),
              child: Row(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Icon(
                    Icons.info_outline,
                    color: Colors.amber,
                  ),
                  SizedBox(width: 12),
                  Expanded(
                    child: Text(
                      "News displayed here is currently sample "
                      "content for the Jibwis AI app. Real-time "
                      "news can be connected through an API later.",
                      style: TextStyle(
                        color: Colors.white70,
                        height: 1.4,
                      ),
                    ),
                  ),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }

  Widget _newsCard({
    required IconData icon,
    required Color iconColor,
    required String category,
    required String title,
    required String description,
    required String time,
  }) {
    return Card(
      margin: const EdgeInsets.only(bottom: 14),
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Row(
              children: [
                CircleAvatar(
                  backgroundColor:
                      iconColor.withOpacity(0.15),
                  child: Icon(
                    icon,
                    color: iconColor,
                  ),
                ),

                const SizedBox(width: 12),

                Expanded(
                  child: Text(
                    category,
                    style: TextStyle(
                      color: iconColor,
                      fontWeight: FontWeight.bold,
                      fontSize: 12,
                    ),
                  ),
                ),

                Text(
                  time,
                  style: const TextStyle(
                    color: Colors.white54,
                    fontSize: 12,
                  ),
                ),
              ],
            ),

            const SizedBox(height: 14),

            Text(
              title,
              style: const TextStyle(
                fontSize: 18,
                fontWeight: FontWeight.bold,
              ),
            ),

            const SizedBox(height: 8),

            Text(
              description,
              style: const TextStyle(
                color: Colors.white70,
                height: 1.5,
              ),
            ),
          ],
       