import 'package:flutter/material.dart';

import '../widgets/dashboard_card.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "Jibwis AI",
          style: TextStyle(
            fontWeight: FontWeight.bold,
          ),
        ),
        centerTitle: true,
        actions: [
          IconButton(
            onPressed: () {},
            icon: const Icon(Icons.notifications_outlined),
          ),
        ],
      ),

      body: ListView(
        padding: const EdgeInsets.all(16),
        children: [
          // Welcome
          const Text(
            "Welcome back 👋",
            style: TextStyle(
              fontSize: 16,
              color: Colors.white70,
            ),
          ),

          const SizedBox(height: 6),

          const Text(
            "Trade smarter with Jibwis AI",
            style: TextStyle(
              fontSize: 24,
              fontWeight: FontWeight.bold,
            ),
          ),

          const SizedBox(height: 20),

          // Account Card
          Card(
            elevation: 4,
            color: const Color(0xFF0D47A1),
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(18),
            ),
            child: const Padding(
              padding: EdgeInsets.all(20),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(
                    "Account Balance",
                    style: TextStyle(
                      color: Colors.white70,
                    ),
                  ),

                  SizedBox(height: 8),

                  Text(
                    "\$10,000.00",
                    style: TextStyle(
                      fontSize: 30,
                      fontWeight: FontWeight.bold,
                      color: Colors.white,
                    ),
                  ),

                  SizedBox(height: 12),

                  Row(
                    children: [
                      Icon(
                        Icons.trending_up,
                        color: Colors.greenAccent,
                      ),
                      SizedBox(width: 6),
                      Text(
                        "+2.45% Today",
                        style: TextStyle(
                          color: Colors.greenAccent,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),
          ),

          const SizedBox(height: 20),

          const Text(
            "Quick Access",
            style: TextStyle(
              fontSize: 20,
              fontWeight: FontWeight.bold,
            ),
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.show_chart,
            title: "Live Market",
            subtitle:
                "Track Forex, Gold and Crypto markets.",
            color: Colors.green,
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.campaign,
            title: "Premium Signals",
            subtitle:
                "View BUY and SELL trading signals.",
            color: Colors.orange,
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.smart_toy,
            title: "AI Analysis",
            subtitle:
                "Ask Jibwis AI to analyze the market.",
            color: Colors.blue,
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.school,
            title: "AI Academy",
            subtitle:
                "Learn Forex and trading step by step.",
            color: Colors.purple,
          ),

          const SizedBox(height: 20),

          const Text(
            "Market Highlights",
            style: TextStyle(
              fontSize: 20,
              fontWeight: FontWeight.bold,
            ),
          ),

          const SizedBox(height: 12),

          _marketCard(
            symbol: "XAU/USD",
            name: "Gold",
            price: "\$2,458.20",
            change: "+1.25%",
            isPositive: true,
          ),

          const SizedBox(height: 10),

          _marketCard(
            symbol: "EUR/USD",
            name: "Euro / Dollar",
            price: "1.1685",
            change: "-0.42%",
            isPositive: false,
          ),

          const SizedBox(height: 10),

          _marketCard(
            symbol: "BTC/USD",
            name: "Bitcoin",
            price: "\$118,450",
            change: "+2.80%",
            isPositive: true,
          ),
        ],
      ),
    );
  }

  Widget _marketCard({
    required String symbol,
    required String name,
    required String price,
    required String change,
    required bool isPositive,
  }) {
    return Card(
      child: ListTile(
        leading: CircleAvatar(
          backgroundColor:
              isPositive ? Colors.green : Colors.red,
          child: Icon(
            isPositive
                ? Icons.trending_up
                : Icons.trending_down,
            color: Colors.white,
          ),
        ),

        title: Text(
          symbol,
          style: const TextStyle(
            fontWeight: FontWeight.bold,
          ),
        ),

        subtitle: Text(name),

        trailing: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          crossAxisAlignment: CrossAxisAlignment.end,
          children: [
            Text(
              price,
              style: const TextStyle(
                fontWeight: FontWeight.bold,
              ),
            ),

            Text(
              change,
              style: TextStyle(
                color: isPositive
                    ? Colors.green
                    : Colors.red,
                fontWeight: FontWeight.bold,
              ),
            ),
          ],
        ),
      ),
    );
  }
}