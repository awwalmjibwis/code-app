import 'package:flutter/material.dart';
import '../widgets/dashboard_card.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "Jibwis AI",
          style: TextStyle(
            fontWeight: FontWeight.bold,
          ),
        ),
        centerTitle: true,
      ),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: [
          const Text(
            "Welcome to Jibwis AI 🤖",
            style: TextStyle(
              fontSize: 24,
              fontWeight: FontWeight.bold,
            ),
          ),

          const SizedBox(height: 8),

          const Text(
            "Your smart trading assistant.",
            style: TextStyle(
              color: Colors.white70,
              fontSize: 15,
            ),
          ),

          const SizedBox(height: 25),

          DashboardCard(
            icon: Icons.show_chart,
            title: "Live Market",
            subtitle:
                "Track Forex & Crypto markets in real time.",
            color: Colors.green,
            onTap: () {},
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.campaign,
            title: "Premium Signals",
            subtitle:
                "Receive high-quality trading signals.",
            color: Colors.orange,
            onTap: () {},
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.smart_toy,
            title: "AI Analysis",
            subtitle:
                "Ask AI to analyze your trading ideas.",
            color: Colors.blue,
            onTap: () {},
          ),

          const SizedBox(height: 12),

          DashboardCard(
            icon: Icons.school,
            title: "AI Academy",
            subtitle:
                "Learn trading step by step.",
            color: Colors.purple,
            onTap: () {},
          ),

          const SizedBox(height: 25),

          Card(
            child: Padding(
              padding: const EdgeInsets.all(18),
              child: Row(
                children: [
                  const Icon(
                    Icons.shield_outlined,
                    color: Colors.amber,
                    size: 32,
                  ),

                  const SizedBox(width: 14),

                  Expanded(
                    child: Column(
                      crossAxisAlignment:
                          CrossAxisAlignment.start,
                      children: const [
                        Text(
                          "Trade With Risk Management",
                          style: TextStyle(
                            fontWeight: FontWeight.bold,
                            fontSize: 16,
                          ),
                        ),
                        SizedBox(height: 5),
                        Text(
                          "Always protect your account and manage your risk.",
                          style: TextStyle(
                            color: Colors.white70,
                            height: 1.4,
                          ),
                        ),
                      ],
                    ),
                  ),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }
}